---
name: doc-build
description: Self-declaring documentation coverage built on msdmd. Canonical upstream is The-Interdependency/skill-lib/doc-build. Load this when adding or auditing METAPAT documentation ownership.
---

# doc-build — local METAPAT skill entry

Canonical upstream: `The-Interdependency/skill-lib/doc-build/SKILL.md`.

METAPAT modules declare documentation ownership in `DOCS` blocks beside source.

Required local fields:

```text
id, summary, audience, source, status
```

Unknowns are `hmmm`, not guessed.
